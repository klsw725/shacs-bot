# 2026-03-16 Progress UX 개선 (SPEC 전체 완료)

## 프롬프트

> D Next - 다음 작업 진행 → PRD 분석 후 `progress-message-cleanup` + `skill-hint` 순차 구현

## 변경 사항

### PRD 1: progress-message-cleanup (변경 1, 2)

#### `shacs_bot/config/schema.py` (line 204)
- `send_progress` 기본값 `True` → `False`
- LLM 독백("잠시만요")이 기본적으로 사용자에게 전달되지 않음

#### `shacs_bot/channels/discord.py` (line 122-124)
- `send()` 메서드의 `finally` 블록에 `_progress` 메타데이터 조건 추가
- Progress 메시지 전송 시 typing indicator 유지
- Telegram/Matrix와 동일한 패턴 적용

### PRD 2: skill-hint (변경 3)

#### `shacs_bot/agent/loop.py`
- `_detect_skill_hint` 정적 메서드 추가 — `read_file` 호출에서 `/skills/{name}/SKILL.md` 패턴 감지
- `_bus_progress` 콜백에 `skill_hint: bool = False` 키워드 인자 추가, `_skill_hint` 메타데이터 설정
- `_run_agent_loop`에서 tool_hint 전송 직후 `_detect_skill_hint` 호출, 감지 시 `on_progress(msg, skill_hint=True)` 전송

#### `shacs_bot/channels/manager.py` (line 192-198)
- `_skill_hint` 메타데이터 확인 시 필터 바이패스 — `send_progress`/`send_tool_hints` 설정과 무관하게 항상 전송

#### `shacs_bot/cli/commands.py`
- 일회성 모드 `_cli_progress`: `skill_hint` 파라미터 추가, 스킬 힌트는 필터 바이패스
- 인터랙티브 모드 `_consume_outbound`: `_skill_hint` 메타데이터 확인, 항상 표시

## 검증

- LSP 진단: 4개 파일 모두 변경 관련 신규 에러 없음 (기존 타입 이슈만 존재)
- 패턴 일치: Discord `_progress` 조건문이 Telegram/Matrix와 동일 구조
- 필터 로직: manager.py/commands.py 양쪽에서 `_skill_hint` 바이패스 일관 적용

### PRD 3: skill-intent-guidance (스킬 활용 의도 안내)

#### `shacs_bot/templates/SOUL.md` (line 17)
- 의도 파악 테이블에 "스킬 활용" 유형 1행 추가
- "기억 회상"과 "파일/시스템 작업" 사이에 배치
- 특정 스킬명 하드코딩 없이 "시스템 프롬프트의 스킬 목록" 참조
- "read_file → exec 순서로 실행할 것" 워크플로 명시

## 검증

- LSP 진단: 4개 파일 모두 변경 관련 신규 에러 없음 (기존 타입 이슈만 존재)
- 패턴 일치: Discord `_progress` 조건문이 Telegram/Matrix와 동일 구조
- 필터 로직: manager.py/commands.py 양쪽에서 `_skill_hint` 바이패스 일관 적용

## 관련 문서

- SPEC: `docs/specs/2026-03-16-progress-ux-개선.md`
- PRD: `docs/prds/progress-message-cleanup.md` (M1 완료)
- PRD: `docs/prds/skill-hint.md` (M1-M3 완료)
- PRD: `docs/prds/skill-intent-guidance.md` (M1 완료)
