# 코드 품질 리팩토링

> **Prompt**: 코드를 지능적으로 리팩토링하고 품질을 개선합니다

## 변경 요약

12개 파일, +131 -263 (순감 132줄). 런타임 버그 2건 수정, 컨벤션 위반 제거, 코드 중복 3건 해소, 스키마 드리프트 해결.

## 변경 상세

### Phase 1: 런타임 버그 수정

| 파일 | 변경 |
|---|---|
| `agent/memory.py` | `MemoryStore.consolidate()` 내 정의되지 않은 변수(`archive_all`, `session`, `memory_window`) 참조하는 데드 코드 블록 제거 (44줄 삭제). 리팩토링 잔재. |
| `cli/commands.py` | `_pick_heartbeat_target()` (async 함수) 호출 2곳에 `await` 추가. TypeError 크래시 방지. |

### Phase 2: 컨벤션 통일

| 파일 | 변경 |
|---|---|
| `providers/litellm.py` | `from typing import Union` 제거. `Union[X, Y]` 3곳 → `X \| Y` 교체. |
| `skills/.../quick_validate.py` | `from typing import Optional` 제거. `Optional[X]` 7곳 → `X \| None` 교체. |
| `config/loader.py` | `print()` 2줄 → `logger.warning()` 교체. loguru import 추가. |

### Phase 3: 코드 중복 제거

| 파일 | 변경 |
|---|---|
| `channels/telegram.py` | 로컬 `_split_message()` 삭제, `utils.helpers.split_message` import 사용 |
| `channels/discord.py` | 로컬 `_split_message()` 삭제, `utils.helpers.split_message` import 사용 |
| `agent/tools/registry.py` | `create_default_tools()` 팩토리 함수 추가 (공용 도구 8개 생성) |
| `agent/loop.py` | `_register_default_tools()` 를 팩토리 호출로 교체. 개별 도구 import 제거 |
| `agent/subagent.py` | 도구 생성 코드를 팩토리 호출로 교체. 개별 도구 import 제거 |
| `channels/manager.py` | `_init_channels()` 10개 if 블록(~120줄) → `_CHANNEL_DEFS` 레지스트리 + 루프(~30줄). `_resolve_extra_kwargs()` 헬퍼 추가 |

### Phase 4: 스키마 동기화

| 파일 | 변경 |
|---|---|
| `config/schema.py` | `ProvidersConfig`에 누락 필드 5개 추가: `azure_openai`, `volcengine_coding_plan`, `byteplus`, `byteplus_coding_plan`, `ollama` |

### Phase 5: 에러 처리 개선

| 파일 | 변경 |
|---|---|
| `agent/loop.py` | MCP cleanup의 `except Exception: pass` → `except (RuntimeError, BaseExceptionGroup): pass` (`close_mcp()`과 동일하게 통일) |

## 검증 결과

- `Union[` 사용: 0건
- `Optional[` 사용: 0건
- `_split_message` 중복: 0건
- `print()` in config/: 0건
- Provider 필드: 5개 정상 추가 확인
- Tool factory: 8개 도구 정상 생성 확인
